name: meu_retroplay
description: "RetroPlay Cloud Gaming App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  webview_flutter: ^4.7.0
  webview_flutter_web: ^0.2.2+4
  cupertino_icons: ^1.0.6
  path_provider: ^2.1.2
  android_intent_plus: ^5.0.2
  url_launcher: ^6.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true